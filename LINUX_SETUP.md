# Linux Development Setup

To build and run the application on Linux Desktop, you need to install the following development tools:

```bash
sudo apt-get update
sudo apt-get install clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev
```

These are required by Flutter to compile the C++ runner for the Linux application.
